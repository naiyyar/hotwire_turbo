<p style="color: green"><%= notice %></p>

<h1>
  Messages
  (<span id='messages-count'><%=@messages.count%></span>)
</h1>

<div id='new-message-form'>
  <%= render 'form', message: Message.new %>
</div>

<div id="messages">
  <%= render @messages %>
</div>