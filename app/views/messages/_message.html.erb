<div id="<%= dom_id message %>">
  <p>
    <strong>Body:</strong>
    <%= message.body %>
  </p>

  <%= button_to "Edit", edit_message_path(message) %>
  <%= button_to "Delete", message, method: :delete %>

</div>
